"""Harness templates — pre-built capability profiles for Clou.

Each module in this package exports a ``template`` attribute of type
``HarnessTemplate``.  The orchestrator loads templates by name via
``clou.harness.load_template()``.
"""
